# Dev/CI tooling only. Not baked into the Docker image (see inference/Dockerfile,
# which installs requirements.txt alone). CI installs this on top to lint + test.
ruff
pytest
httpx   # required by fastapi.testclient.TestClient
