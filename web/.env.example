# Where the API lives. Optional: env.ts already defaults to
# http://localhost:8080 in dev and to /api in production builds (nginx proxies
# it), so you only need this to point the dev server at a remote API.
VITE_API_URL=http://localhost:8080
